extends /0_basics/_default
block content
  .container
    .row
      .col-lg-12
        h1 Demo of the <%= moduletype %>: <%= modulenameCamelized %>
        <% if (markupmixins) { %>
        +<%= modulenameCamelized %>(yaml.<%= modulenameCamelized %>)
        <% } %>
