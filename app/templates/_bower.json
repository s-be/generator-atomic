{
  "name": "<%= appname %>",
  "version": "0.0.0",
  "dependencies": {
    "jquery": "^2.1.4",<% if (cssPreprocessor === 'less') { %>
    "bootstrap": "^3",<% } %><% if (cssPreprocessor === 'sass') { %>
    "bootstrap": "^4",<% } %>
    "lodash": "^3.10.1"
  },
  "devDependencies": {}
}

