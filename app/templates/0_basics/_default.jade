//- [injector:jademixins]
//- [endinjector]
doctype html
html
  head
    title= lang.meta.title
    meta(charset='UTF-8')
    meta(content=lang.meta.description, name="description")
    meta(content=lang.meta.keywords, name="keywords")
    meta(content='<%= author %>', name='author')
    meta(content='3 days', name='revisit-after')
    meta(name='viewport', content="width=device-width, initial-scale=1, minimum-scale=1, maximum-scale=1, user-scalable=no")
    link(type="text/plain" rel="author" href="#{path}humans.txt")

    link(href='#{path}0_basics/main.css', rel='stylesheet')

    noscript
      link(href='#{path}0_basics/nojs.css', rel='stylesheet')
    <!--[if lte IE 9]>
    <link href="#{path}0_basics/ie9.css" rel="stylesheet">
    <![endif]-->
  body(class=page)
    +header
    block content
    +footer
    if level == 0
      // build:js({.,.tmp}) 0_basics/controller.js
    else if level == 1
      // build:js({.,.tmp}) ../0_basics/controller.js
    else
      // build:js({.,.tmp}) ../../0_basics/controller.js

    // bower:js
    script(src='/jquery/dist/jquery.js')
    // endbower

    // [injector:js]
    // [endinjector]

    script(src='#{path}0_basics/controller.js')
    // endbuild
